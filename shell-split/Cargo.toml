[package]
name = "uefi-shell-split"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true

[features]
alloc = []
uefi = ["dep:uefi"]

[dependencies]
uefi = { version = "0.24.0", default-features = false, optional = true }
